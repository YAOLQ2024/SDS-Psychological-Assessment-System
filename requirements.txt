# 启智AI板子 NPU加速依赖配置 - Python 3.9.2兼容版本
# 注意：此文件已调整为兼容已安装的包版本，避免降级冲突
# 如果包已安装，将使用已安装的版本；如果未安装，将安装指定版本

# 基础Web框架
Flask>=2.2.5  # 如果已安装更新版本，使用已安装版本
Werkzeug>=2.2.3
Jinja2>=3.1.2
click>=8.1.3
itsdangerous>=2.1.2
MarkupSafe>=2.1.3
blinker>=1.6.0  # Flask 3.x 需要此依赖

# ARM64优化的科学计算库 - 兼容已安装的更新版本
# 注意：numpy 1.26.4已安装，scipy 1.13.1已安装，不强制降级
numpy>=1.24.3  # 兼容1.24.3及以上，已安装1.26.4
scipy>=1.10.1  # 兼容1.10.1及以上，已安装1.13.1

# 音频处理库 - Python 3.9兼容版本
librosa>=0.9.2
soundfile>=0.12.1
resampy>=0.4.2
numba>=0.56.4  # 兼容Python 3.9的版本
llvmlite>=0.39.1  # 兼容numba

# PyTorch和音频处理（WeNet ASR需要）
# 注意：torch 2.1.0和torchaudio 2.1.0已安装，满足要求
torch>=1.13.0  # 已安装2.1.0，满足要求
torchaudio>=0.13.0  # 已安装2.1.0，满足要求

# 音频格式支持
ffmpeg-python>=0.2.0
pydub>=0.25.1

# 深度学习框架 - 兼容已安装版本
scikit-learn>=1.2.2  # 已安装1.6.1，满足要求
joblib>=1.2.0  # 已安装1.5.0，满足要求

# 数据处理 - 兼容已安装版本
pandas>=1.5.3  # 已安装2.2.3，满足要求

# 图像和信号处理 - 兼容已安装版本
opencv-python>=4.7.0  # 已安装4.8.0.76，满足要求
Pillow>=9.5.0  # 已安装10.4.0，满足要求

# 系统监控和工具 - 兼容已安装版本
psutil>=5.9.4  # 已安装7.0.0，满足要求
requests>=2.28.2  # 已安装2.32.2，满足要求
urllib3>=1.26.15  # 已安装2.5.0，满足要求

# 日志和配置
pyyaml>=6.0  # 已安装6.0.2，满足要求
configparser>=5.3.0

# 开发和调试工具 - 兼容已安装版本
tqdm>=4.64.1  # 已安装4.67.1，满足要求

# Web服务器
gunicorn>=20.1.0
eventlet>=0.33.3

# 安全和认证
cryptography>=40.0.2  # 已安装38.0.4，可能需要升级
bcrypt>=4.0.1

# API和文档
flask-restx>=1.1.0
flask-cors>=4.0.0

# 文本处理和NLP - Python 3.9兼容版本
jieba>=0.42.1
pypinyin>=0.48.0

# 文件和数据格式
openpyxl>=3.1.1
h5py>=3.8.0
jsonschema>=4.17.3  # 已安装4.23.0，满足要求

# 系统集成
schedule>=1.2.0
watchdog>=3.0.0

# 测试相关的依赖 - 兼容已安装版本
decorator>=5.1.1  # 已安装5.2.1，满足要求
attrs>=22.2.0  # 已安装25.3.0，满足要求
cloudpickle>=2.2.1  # 已安装3.1.1，满足要求
tornado>=6.2  # 已安装6.5.1，满足要求
absl-py>=1.4.0  # 已安装2.2.2，满足要求

# 额外的必需依赖 - 兼容已安装版本
packaging>=23.0  # 已安装25.0，满足要求
certifi>=2022.12.7
charset-normalizer>=3.0.0  # 已安装2.0.4，可能需要升级
idna>=3.4  # 已安装2.8，可能需要升级
platformdirs>=3.0.0  # 已安装4.3.8，满足要求

# 表情识别相关依赖
easydict>=1.10  # 配置管理
PyYAML>=6.0  # YAML配置文件解析（已安装6.0.2）
py-cpuinfo>=9.0.0  # CPU信息获取
